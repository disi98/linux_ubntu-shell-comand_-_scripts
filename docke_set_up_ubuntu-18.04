#!/bin/bash
sudo apt update && sudo apt-get install \
    ca-certificates \
    curl \
    gnupg \
    lsb-release && sudo  curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
